const assert = require('chai').assert;
const Tron = require('../lib/index.js')

describe('lets test some shit, bruh', () => {

  it(' coordinateArray should be an array', () => {
    var game = new Tron;

    beginning()
    assert.isArray(coordinateArray)
  })
})
