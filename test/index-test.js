const assert = require('chai').assert;
const Game = require('../lib/index.js')

describe('lets test some shit, bruh', () => {

  it(' coordinateArray should be an array', () => {
    var game = new Game;
    assert.isArray()
  })
})
